<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlaylistMaker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:background">@color/main_activity_background</item>
    </style>

    <style name="PlaylistMakerActivitySetting" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:colorPrimary">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="PlaylistMakerActivitySettingHeader" parent="PlaylistMakerActivitySetting">
        <item name="drawableStartCompat">@drawable/ic_arrow</item>
    </style>

    <style name="PlaylistMakerActivitySearch" parent="PlaylistMakerActivitySetting">
        <item name="drawableStartCompat">@drawable/ic_magnifier_light</item>
    </style>

</resources>